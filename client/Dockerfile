FROM node:14-alpine

WORKDIR /app
COPY package*.json .
RUN npm install

COPY . .


EXPOSE 8080
ENV HOST=0.0.0.0
ENV PORT=8080

ARG NUXT_APP_BACKEND_URL
ENV NUXT_APP_BACKEND_URL=$NUXT_APP_BACKEND_URL
ARG NUXT_APP_LSP_URL
ENV NUXT_APP_LSP_URL=$NUXT_APP_LSP_URL

CMD ["npm", "run", "dev"]